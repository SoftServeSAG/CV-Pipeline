[pytest]
markers =
    seresnext: test seresnext model
    resnet: test resnet model
    densenet: test densenet model
    espnet: test espnet model
    validator: validate config
    metric_utils: test metric utils
    losses: test losses utils
    models: run only models tests

addopts = --cov=../model_training/ --cov-report html -v
